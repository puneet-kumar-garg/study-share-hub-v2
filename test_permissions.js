// Test script for permissions system
// Run this in browser console after logging in as admin

import { grantUploadPermission, canUserUpload } from './src/lib/permissions';\n\n// Test granting permission to a new user\nasync function testPermissions() {\n  const testEmail = 'test@example.com';\n  const adminEmail = 'puneet@gmail.com';\n  \n  console.log('Testing permissions system...');\n  \n  // Test 1: Grant permission\n  console.log('1. Granting permission to', testEmail);\n  const grantResult = await grantUploadPermission(testEmail, adminEmail);\n  console.log('Grant result:', grantResult);\n  \n  // Test 2: Check if user can upload\n  console.log('2. Checking if user can upload');\n  const canUpload = await canUserUpload(testEmail);\n  console.log('Can upload:', canUpload);\n  \n  // Test 3: Check admin permissions\n  console.log('3. Checking admin permissions');\n  const adminCanUpload = await canUserUpload(adminEmail);\n  console.log('Admin can upload:', adminCanUpload);\n}\n\n// Run the test\ntestPermissions().catch(console.error);